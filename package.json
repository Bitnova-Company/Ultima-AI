{
  "name": "ultima-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Ultima AI — the Model Change Intelligence Network. Detect silent LLM provider changes, corroborate them across tenants, repair the blast radius, and prove it to an auditor.",
  "license": "BUSL-1.1",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node apps/api/src/server.js",
    "dev": "node --watch apps/api/src/server.js",
    "seed": "node apps/api/src/seed.js",
    "test": "node --test --experimental-test-coverage packages/core/test/*.test.js",
    "verify": "node scripts/verify-evidence.js"
  }
}
